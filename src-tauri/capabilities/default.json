{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tilde's window needs the core APIs and nothing else. The dialog and opener plugins are called from Rust, never from the page, so the web layer is not granted them.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
